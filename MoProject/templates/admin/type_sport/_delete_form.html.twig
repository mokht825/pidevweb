<form method="post" action="{{ path('type_sport_delete', {'id': type_sport.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_sport.id) }}">
    <button class="btn btn-danger btn-xs"><i class="fa fa-trash-o "></i></button>
</form>
